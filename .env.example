# === Required ===
# Base URL of your abs-stats instance
ABSSTATS_BASE_URL=http://abs-stats:3000

# === Optional: Polling & Data ===
POLL_SECONDS=300
STATE_DB_PATH=/data/state.db
ACHIEVEMENTS_PATH=/data/achievements.points.json
SERIES_REFRESH_SECONDS=86400
COMPLETED_ENDPOINT=/api/completed
ALLOW_PLAYLIST_FALLBACK=true

# === Optional: Discord Notifications ===
# URL to a Discord webhook proxy (e.g. abs-stats discord-notify endpoint)
# Leave empty to disable Discord notifications.
DISCORD_PROXY_URL=

# === Optional: User Display Names ===
# Comma-separated mapping of ABS usernames to friendly display names.
# Used in Discord notifications and the web dashboards.
# Format: absuser1:Display Name,absuser2:Display Name
USER_ALIASES=

# === Optional: User Avatar Icons ===
# Comma-separated mapping of ABS usernames to avatar icon paths.
# Place avatar PNGs in the icons/ directory and reference them here.
# Format: absuser1:/icons/avatar1.png,absuser2:/icons/avatar2.png
USER_ICONS=

# === Optional: Email Notifications (SMTP) ===
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=
SMTP_TO_OVERRIDE=
SEND_TEST_EMAIL=false

# === Optional: Per-User Email Mapping ===
# Comma-separated mapping of usernames to email addresses.
# Format: absuser1:user1@example.com,absuser2:user2@example.com
USER_EMAILS=
